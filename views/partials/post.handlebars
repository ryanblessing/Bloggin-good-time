<article class="post">
    <div class="title">
        <h3>{{title}}</h3>
    </div>
    <div class="content">
        <h4>{{content}}</h4>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    </div>
</article>